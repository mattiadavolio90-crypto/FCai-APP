DA LANCIARE IN CMD
pip install streamlit pandas xmltodict openai plotly openpyxl pdf2image Pillow


📋 Lista Dettagliata dei Componenti
Ecco a cosa serve ogni "pezzo" del motore della tua app:

streamlit: Il telaio dell'auto. Crea il sito web, i bottoni e l'interfaccia.

pandas: Il motore di calcolo. Gestisce le tabelle dati, fa le somme e le analisi.

xmltodict: L'interprete. Traduce le fatture elettroniche (XML) in un formato che Python può leggere.

openai: Il cervello. Collega la tua app all'intelligenza artificiale per capire le categorie dei prodotti.

plotly: Il pittore. Disegna i grafici a barre colorati e interattivi.

openpyxl: Il ragioniere (Base). Legge e scrive file Excel standard.

xlsxwriter: Il ragioniere (Avanzato). Serve per creare file Excel con formattazioni complesse (utile per sviluppi futuri).

pip install pdf2image
pip install Pillow
